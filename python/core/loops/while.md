# `while` loop`



## Examples

### Loop until there is an item, `break` if there is an exception

~~~~
counter = =
while item:
    try:
        item = self.__next__()
        counter += 1
    except:
        break
~~~~
